apiVersion: apps/v1
kind: Deployment
metadata:
  name: aukro-service
  namespace: statex-apps
  labels:
    app: aukro-service
    managed-by: k8s-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aukro-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: aukro-service
    spec:
      containers:
        - name: app
          image: localhost:5000/aukro-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3700
          envFrom:
            - configMapRef:
                name: aukro-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-credentials
                  key: DB_PASSWORD
          livenessProbe:
            httpGet:
              path: /health
              port: 3700
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 3700
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
